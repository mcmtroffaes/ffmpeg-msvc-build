cmake_minimum_required(VERSION 3.17)
project(test_avformat)
find_package(FFMPEG REQUIRED)
add_executable(test_readframe readframe.cpp)
target_link_libraries(test_readframe PRIVATE ${FFMPEG_LIBRARIES})
target_include_directories(test_readframe PRIVATE ${FFMPEG_INCLUDE_DIRS})